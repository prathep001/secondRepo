pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout completed'
            }
        }
        stage('Filter') {
            steps {
                echo 'Filter completed'
            }
        }
    }
}
